{
  "name": "salon-sms-reminder",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "SMS appointment reminders for a hair salon, driven entirely by Google Calendar",
  "scripts": {
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "vitest": "^5.0.1",
    "wrangler": "^4.136.1"
  }
}
